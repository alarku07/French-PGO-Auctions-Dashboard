---
description: General development rules
alwaysApply: true
---
# Project files structure
- At project root folder is `src` folder where all source code will be
- backend code will be in `src/backend` folder
- frontend code will be in `src/frontend` folder
- tests are in `tests` folder
- docs are `docs` folder
